
#include "../utils/tracers.h"
//#include <pbrt/src/pbrt/shapes.h>

int main(){
    //pbrt::Transform t = pbrt::RotateX(2);
    //pbrt::Transform inverse = pbrt::Transform(t.GetInverseMatrix());
    //pbrt::Sphere esfera(&t,&inverse,true,1,-1,1,360);

    return 0;
}